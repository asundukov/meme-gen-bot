ALTER TABLE meme
    add column width int NOT NULL DEFAULT 0;
ALTER TABLE meme
    add column height int NOT NULL DEFAULT 0;

ALTER TABLE meme
    add column thumb_width int NOT NULL DEFAULT 0;
ALTER TABLE meme
    add column thumb_height int NOT NULL DEFAULT 0;
